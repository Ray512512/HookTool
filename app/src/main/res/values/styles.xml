<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar" >
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBackGround</item>
        <item name="colorPrimaryDark">@color/primaryBackDark</item>
        <item name="colorAccent">@color/color_green3</item>
        <item name="colorControlNormal">@color/color_green3</item>
        <item name="colorControlActivated">@color/colorBackGround</item>
        <item name="colorControlHighlight">@color/color_green3</item>
    </style>

</resources>
